#include<bits/stdc++.h>

using namespace std;
const int N = 1e5 + 5;
int a[N];
void solve(){
    int n, q, x, y;
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> a[i];
    }         
    cin >> q;
    while(q--){
        cin >> x >> y;
    }

}
int main(){
    ios::sync_with_stdio(false);
    solve();
    return;
}